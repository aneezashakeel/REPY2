for fn in ("len.txt", "len.txt.a", "len.txt.b"):
    if fn in listfiles():
        removefile(fn)

h = ABopenfile("len.txt", True)
h.writeat("SAFE", 0)
h.close()

h = ABopenfile("len.txt", False)
h.writeat("LONGDATA", 0)
h.close()

h = ABopenfile("len.txt", False)
h.writeat("SHORT", 0)
h.close()

r = ABopenfile("len.txt", False)
d = r.readat(None, 0)
r.close()
