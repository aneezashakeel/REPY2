if "b.txt" in listfiles():
    removefile("b.txt")
if "b.txt.a" in listfiles():
    removefile("b.txt.a")
if "b.txt.b" in listfiles():
    removefile("b.txt.b")

myfile = ABopenfile("b.txt", True)
myfile.writeat("SXXXE", 0)
myfile.writeat("Y", 2)
myfile.close()
