for fn in ("length.txt", "length.txt.a", "length.txt.b"):
    if fn in listfiles():
        removefile(fn)

myfile = ABopenfile("length.txt", True)
myfile.writeat("THISISAVERYLONGINVALIDSTRING", 0)
myfile.close()

myfile = ABopenfile("length.txt", False)
myfile.writeat("SE", 0)
myfile.close()
