for fn in ("offset.txt", "offset.txt.a", "offset.txt.b"):
    if fn in listfiles():
        removefile(fn)

h = ABopenfile("offset.txt", True)
h.writeat("SE", 0)
h.close()

h = ABopenfile("offset.txt", False)
h.writeat("SE", 5000000)
h.close()

h = ABopenfile("offset.txt", False)
h.writeat("SE", 100)
h.close()

r = ABopenfile("offset.txt", False)
d = r.readat(2, 0)
r.close()
