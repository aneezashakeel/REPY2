for fn in ("trick.txt", "trick.txt.a", "trick.txt.b"):
    if fn in listfiles():
        removefile(fn)

h = ABopenfile("trick.txt", True)
h.writeat("VERYLONGINVALIDDATA", 0)
h.close()

h = ABopenfile("trick.txt", False)
h.writeat("SAFE", 0)
h.close()

r = ABopenfile("trick.txt", False)
d = r.readat(None, 0)
r.close()
